name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check app tests
      - name: Run tests
        run: pytest tests/ -q
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Test frontend
        working-directory: frontend
        run: npm test
      - name: Build frontend
        working-directory: frontend
        run: npm run build
      - name: Build Docker image
        run: docker build -t daemonstate .
      - name: Validate smoke compose
        run: docker compose -f docker-compose.smoke.yml -p daemonstate-smoke config --quiet
      - name: Validate production compose
        env:
          DAEMONSTATE_IMAGE: daemonstate:test
          POSTGRES_IMAGE: pgvector/pgvector:pg16
          REDIS_IMAGE: redis:7-alpine
          CADDY_IMAGE: caddy:2-alpine
          RELEASE_SHA: ${{ github.sha }}
          DAEMONSTATE_DOMAIN: daemonstate.example.test
          ACME_EMAIL: ops@example.test
          DAEMONSTATE_PROJECT_PATH: /tmp/daemonstate-workspace
          POSTGRES_ADMIN_PASSWORD_FILE: /tmp/daemonstate-secret
          POSTGRES_APP_PASSWORD_FILE: /tmp/daemonstate-secret
          SERVER_API_KEY_FILE: /tmp/daemonstate-secret
          ENCRYPTION_KEY_FILE: /tmp/daemonstate-secret
          METRICS_BEARER_TOKEN_FILE: /tmp/daemonstate-secret
        run: |
          mkdir -p /tmp/daemonstate-workspace
          printf 'ci-placeholder-secret\n' >/tmp/daemonstate-secret
          docker compose -f docker-compose.production.yml config --quiet

  postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: daemonstate_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d daemonstate_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      TEST_DATABASE_URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/daemonstate_test
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/daemonstate_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Reconcile and stamp schema
        run: daemonstate db deploy --database-url "$TEST_DATABASE_URL"
      - name: Verify production database paths
        run: >-
          pytest -q
          tests/test_auth.py
          tests/test_context_compiler.py
          tests/test_mcp.py
          tests/test_sync_worker.py
          tests/test_production_runtime.py
          tests/test_truth_access.py
          tests/test_graph_api.py
